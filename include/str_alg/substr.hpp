#pragma once
#include <string>
#include <vector>

namespace rpt {
std::vector<int> prefix_function(const std::string &s);
int substring_index(const std::string &text, const std::string &substring);
} // namespace rpt